name: Package Latest Krishna Defence ZIP

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/package-latest-zip.yml'
  pull_request:
    branches: [main]
    paths:
      - 'package-trigger.txt'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Build clean complete ZIP from exact main
        run: |
          set -euo pipefail
          git fetch origin main --force
          git archive --format=zip --prefix=KrishnaDefence_Integrated_V15_Latest/ -o KrishnaDefence_Integrated_V15_Latest.zip origin/main
          sha256sum KrishnaDefence_Integrated_V15_Latest.zip > KrishnaDefence_Integrated_V15_Latest.zip.sha256
          unzip -t KrishnaDefence_Integrated_V15_Latest.zip
      - name: Upload complete package
        uses: actions/upload-artifact@v4
        with:
          name: KrishnaDefence_Integrated_V15_Latest
          path: |
            KrishnaDefence_Integrated_V15_Latest.zip
            KrishnaDefence_Integrated_V15_Latest.zip.sha256
          if-no-files-found: error
          compression-level: 0
